# This is a basic workflow to help you get started with Actions and add recent activity to the profile

name: Update README

on:
  schedule:
    - cron: '*/30 * * * *'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    name: Update this repo's README with recent activity

    steps:
      - uses: actions/checkout@v2
      - uses: s-earnest/github-activity-readme@master
        env:
          GITHUB_TOKEN: ${{ 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 }}
